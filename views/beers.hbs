<div id="all-beers">
    {{#each data}}
        <div class="beer">
            <div class="beer-image">
                <img src={{this.image_url}} alt={{this.name}}>
            </div>
            <h2>{{this.name}}</h2>
            <h3>{{this.tagline}}</h3>
        </div>
    {{/each}}
</div>